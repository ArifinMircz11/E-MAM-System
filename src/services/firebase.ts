/**
 * Mock / Safe Firebase client wrapper for offline-first architecture
 */

export const auth = {
  currentUser: null,
};

export const db = {};

export const app = {};

export const isFirebaseInitialized = () => false;
