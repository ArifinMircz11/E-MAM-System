import fs from 'node:fs'; import path from 'node:path';
const root=process.cwd(); const targets=['src/features/auth/Login.tsx','src/app/App.tsx','src/services/authService.ts','src/core/security/contextHelper.ts','src/core/identity/security-context/PolicyResolver.ts','src/core/impersonation/ImpersonationService.ts','api/auth/routes.ts','firestore.rules'];
const forbidden=[{pattern:/30315537/g,rule:'hardcoded tenant fallback'},{pattern:/developerBypass|secretClicks|Developer Bypass/g,rule:'production developer bypass'},{pattern:/x-requester-uid/g,rule:'unverified requester identity header'},{pattern:/!hasUserDoc\(\)/g,rule:'tenant authorization accepts missing CanonicalUser'},{pattern:/allow\s+(read|write|read,\s*write):\s*if\s+isSignedIn\(\)/g,rule:'authenticated-only Firestore authorization'},{pattern:/tenantId\s*[:=]\s*['"](?:global|default|unknown)['"]/g,rule:'legacy tenant fallback'},{pattern:/tenantId\s*\|\|\s*['"](?:global|default|unknown)['"]/g,rule:'legacy tenant fallback expression'}];
let failures=0; for(const relative of targets){const file=path.join(root,relative);if(!fs.existsSync(file))continue;const content=fs.readFileSync(file,'utf8');for(const c of forbidden){const m=content.match(c.pattern);if(m?.length){failures+=m.length;console.error(`SECURITY: ${relative}: ${c.rule} (${m.length})`)}}}
if(failures){console.error(`Login security audit FAILED: ${failures} violation(s).`);process.exit(1)} console.log('Login security audit PASSED.');
